Generate
========

Virtual Environment:

```bash
virtualenv .env
source .env/bin/activate
pip install -r requirements.txt
```

Local:

```bash
make html
```

Deploy:

```bash

<edit rst files>
commit
make gh-pages
```
